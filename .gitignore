# Dependencies / build
node_modules/
*.log

# Environment / secrets — never commit
.env
.env.*
*.pem
*.key
secrets.*
tokens.*

# API specs and engagement data — keep client material out of the repo
data/
specs/
*.swagger.json
swagger.json
swagger.*.json
openapi.json
openapi.*.json
*.har

# Editor / OS
.DS_Store
.vscode/
.idea/
